/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "plus.unsplash.com",
      "velog.velcdn.com",
      "avatars.githubusercontent.com",
    ],
  },
};

export default nextConfig;
